function ipv4NetworkCalculator(ip, mask='', cidr='', rede='', broadcast='', num_ips=0, classe='') {
    /* return {
        ip, mask, cidr, rede, broadcast, num_ips, classe,
        contain_ip() {
            if ( this.ip == '' ) {
                throw 'IP não enviado'
            }
        }
    } */
}